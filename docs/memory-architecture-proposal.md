# 记忆架构设计方案 v2（讨论中）

> 状态：v2 方案整合完成，待亚臣最终确认后落地实施
> 更新：2026-03-10，整合 6 篇社区实践文章的核心洞察 + Ally 归属决策

---

## 一、当前问题

现有 MEMORY.md 是个空壳，缺乏结构：
- 所有东西往一个文件塞，没有分层
- 没有反思机制，只记事实不回顾判断
- 没有检索优先级，启动时一股脑全读
- 私有记忆和共享记忆的边界模糊
- 没有生命周期管理，记忆只增不减
- 没有索引机制，Agent 无法按需检索
- Ally（知心伙伴）长期运行但未纳入团队架构

---

## 二、设计参考

v2 方案整合了以下社区实践的核心洞察：

| 来源 | 核心贡献 |
|------|---------|
| @karry_viber | 三层管线（自动提取→反思→归档晋升）、P0/P1/P2 分级、四维混合打分 |
| @wangray | NOW.md 工作缓冲区、温度衰减模型、CRUD 验证防记忆幻觉、多 Agent 记忆隔离 |
| @onehopeA9 | L0/L1/L2 检索分层、`.abstract` 索引机制、P0/P1/P2 生命周期、记忆检索协议 |
| @ohxiyu | 隐私安全分层、vault 脱敏机制、敏感信息永不上云 |
| @seekjourney | self-improving-agent 自学习模式、Markdown 记忆载体、经验自动晋升 |
| @GitHub_Daily | memory-lancedb-pro 生产级检索、语义+关键词混合、自我反思纠错 |

---

## 三、团队架构：4+1+1

原 4+1 架构升级为 **4+1+1**，增加独立的内在系统：

```
工作系统（创造）              生活系统（运维）        内在系统（成长）
┌────────────────────┐      ┌──────────┐          ┌──────────┐
│ Radar → Partner    │      │          │          │          │
│   ↓       ↓        │      │  Butler  │          │   Ally   │
│ Workshop → Keeper  │      │          │          │          │
└────────────────────┘      └──────────┘          └──────────┘
         ↑                        ↑                    ↑
      你(创作者)             你(被照顾的人)        你(成长中的人)
```

三个系统各自独立，互不交互：
- **工作系统**：你是主角，agent 是团队
- **生活系统**：你是甩手掌柜，Butler 打理一切
- **内在系统**：你是对话者，Ally 是认知镜子

### Ally 独立的理由（第一性原理检验）

| 检验维度 | Ally vs Partner | Ally vs Butler |
|---------|----------------|----------------|
| 上下文冲突 | 是。Partner 需要工作决策上下文，Ally 需要情感/认知/生活上下文 | 是。Butler 管事务，Ally 管内在 |
| 自主权不同 | 是。Partner 输出 brief，Ally 输出洞察 | 是。Butler 高自主（自动安排），Ally 跟着你的节奏走 |
| 时间节奏不同 | 是。Ally 可能在任何时刻被需要 | 是。Butler 定时推送，Ally 随时响应 |
| 失败代价不同 | 是。Partner 错了影响工作方向，Ally 错了影响情绪和认知——更私密、更不可逆 | 是。Butler 错了重新安排，Ally 错了可能强化盲点 |

### Ally 的定位

| 维度 | 定义 |
|------|------|
| 本质 | 认知镜子、成长伙伴 |
| 来源 | 基于"知心伙伴 v6.0"创建的 OpenClaw 实例，已长期运行验证 |
| 自主权 | 低——必须跟着亚臣的节奏，不主动发起 |
| 时间节奏 | 随时响应，无固定 heartbeat |
| 输出物 | 洞察、情感解读、认知引导、盲点揭示 |
| 核心价值 | 看见、接纳、不评判；把模糊感受变清晰语言，砸碎认知枷锁 |
| 渗透范围 | 跨工作和生活——内省本身不区分场景 |

---

## 四、私有记忆架构（每个 Agent）

```
Agent目录/
├── .abstract              ← L0 索引（100-300 tokens，启动时第一个读）
├── MEMORY.md              ← L1 长期稳定认知（带 P0/P1/P2 生命周期标签）
├── SESSION-STATE.md       ← 当前工作缓冲区（heartbeat 覆写，context compaction 救生筏）
├── reflections/           ← L2 原始反思日志
│   ├── 2026-03-10.md
│   └── archive/           ← 已提炼的过期反思
└── lessons/               ← 结构化经验教训（从 reflections 提炼）
```

### 各文件职责

| 文件 | 性质 | 谁写 | 谁读 | 更新频率 |
|------|------|------|------|---------|
| `.abstract` | 记忆目录页（索引） | 自己 | 自己 | 每周/每月刷新 |
| `MEMORY.md` | 长期稳定认知 | 自己 | 只有自己 | 每周从 reflections/lessons 提炼 |
| `SESSION-STATE.md` | 当前工作状态 | 自己 | 只有自己 | 实时覆写 |
| `reflections/` | 原始反思日志 | 自己 | 只有自己 | session 驱动 |
| `lessons/` | 结构化经验 | 自己 | 只有自己 | 从 reflections 提炼 |

### 三层私有记忆的硬边界

三层之间**只晋升结论，不复制内容**。晋升后，下层原文可归档或删除。

| 层 | 存什么 | 不存什么 | 检验问题 |
|---|--------|---------|---------|
| `reflections/` | 原始观察：当天发生了什么、我注意到什么、我的即时反应 | 不存结论、不存规则 | "这是今天才观察到的吗？" → 是则写这里 |
| `lessons/` | 可复用经验：带上下文的操作指南，说清"在什么情况下、做什么、为什么" | 不存原始事件细节，不存脱离了上下文也成立的普适认知 | "下次遇到类似情况，翻这篇能直接用吗？" → 是则写这里 |
| `MEMORY.md` | 长期稳定认知：脱离了具体事件仍然成立的原则、偏好、模式 | 不存操作步骤，不存具体案例 | "即使忘了所有具体事件，这条认知还成立吗？" → 是则写这里 |

**举例说明流转过程：**

```
reflections/2026-03-10.md:
  "今天给哈恩写详情页文案，第一版被亚臣打回，原因是首屏没有直接点出核心功效，
   而是用了一段品牌故事铺垫。亚臣说'用户 3 秒内看不到功效就划走了'。"

  ↓ 提炼为 lesson（保留上下文，可复用）

lessons/详情页首屏经验.md:
  "电商详情页首屏必须在前 3 秒内传达核心功效，品牌故事铺垫放在第二屏之后。
   原因：用户注意力窗口极短，首屏是唯一的留人机会。来源：哈恩详情页项目。"

  ↓ 提炼为 MEMORY.md 条目（脱离上下文的稳定认知）

MEMORY.md #P0:
  "亚臣的交付偏好：所有面向消费者的内容，功效信息必须前置，品牌叙事后置。"
```

**反重复规则：** 一条认知晋升到上层后，下层不再保留同义表述。如果 lessons/ 中的某条经验已被完整提炼进 MEMORY.md，该条目从 lessons/ 中删除或标记为"已晋升"。

---

## 五、生命周期管理（P0/P1/P2）

MEMORY.md 中每条记忆带生命周期标签和验证时间戳：

| 级别 | 含义 | 有效期 | 过期处理 |
|------|------|--------|---------|
| **P0** | 永久（身份、核心偏好、长期原则） | 永不过期 | 仅人工修正 |
| **P1** | 活跃项目/阶段性认知 | 90 天 | 到期降级或归档 |
| **P2** | 临时/待验证 | 30 天 | 到期归档 |

### MEMORY.md 条目格式

```markdown
# MEMORY.md — Radar

> 最后提炼：2026-03-10 | 版本：v1

## P0 — 永久
- 亚臣关注领域权重：品牌设计 > 营销策略 > AI 工具 > 行业趋势
  <!-- verified: 2026-03-08 -->

## P1 — 90天
- 哈恩品牌当前处于详情页优化阶段，重点关注竞品视觉差异
  <!-- verified: 2026-03-05 | expires: 2026-06-05 -->

## P2 — 30天
- 某竞品最近在调整主图风格，可能是测试阶段
  <!-- verified: 2026-03-09 | expires: 2026-04-09 -->
```

超过有效期未被验证/引用的条目 → 降级或归档到 `reflections/archive/`。

---

## 六、每个 Agent 的 MEMORY.md 存什么

| Agent | P0 长期认知 | P1/P2 阶段性 | 不存什么 |
|-------|-----------|-------------|---------|
| **Radar** | 信息源质量评估、亚臣的关注领域权重、过滤经验 | 当前监控重点、近期发现的信息源 | 具体新闻内容（→ 共享层竞品档案） |
| **Partner** | 亚臣的决策风格、思维偏好、反复出现的关注点 | 当前讨论中的战略方向 | 具体战略结论（→ 共享层战略决策记录） |
| **Workshop** | 生产标准、brief 常见歧义、亚臣的交付偏好 | 当前项目的特殊要求 | 具体交付物内容（→ 共享层交付物存档） |
| **Keeper** | 审核标准演化、高频问题类型、被推翻的否决反思 | 近期法规变动、临时审核要求 | 具体法规条文（→ 共享层审核规则库） |
| **Butler** | 生活习惯、偏好、重要日期、分类规则 | 近期生活安排 | 具体财务数额（永不落盘） |
| **Ally** | 亚臣的认知模式、价值观演化、反复出现的思维陷阱、成长里程碑 | 当前阶段的困惑/焦虑主题、正在突破的认知瓶颈 | 具体工作决策（→ Partner）、具体生活事务（→ Butler） |

### 脱敏规则

**Butler：**

| 永不落盘 | 可落盘 |
|---------|--------|
| 银行卡号、身份证号、具体金额、密码、API Key | 支出分类比例、资产配置结构、消费趋势、日期 |

**Ally：**

| 永不落盘 | 可落盘 |
|---------|--------|
| 涉及第三方的私密细节、他人隐私 | 情绪模式、认知转变、盲点类型、成长节点（脱敏后） |

---

## 七、.abstract 索引机制

每个 Agent 的 `.abstract` 文件是记忆系统的"目录页"，控制在 100-300 tokens 以内。

### 格式示例

```markdown
# .abstract — Radar

## 记忆概览
- MEMORY.md: 3条P0(信息源/关注领域/过滤经验) + 2条P1(活跃项目) + 1条P2
- SESSION-STATE.md: 当前监控任务状态
- reflections/: 最近7天有5篇，主题集中在竞品动态
- lessons/: 2篇（信息源筛选经验、误报处理）

## 关键词索引
信息源质量 → MEMORY.md#P0
竞品动态 → reflections/2026-03-09.md, lessons/竞品监控经验.md
```

**核心原则：Agent 启动时先读 .abstract，再按需决定读什么，不是一股脑全读。**

---

## 八、共享记忆层（Obsidian）

### 位置

`03_Work/AI-Team/`（Obsidian Vault 内，已同步到 git 仓库）

### 目录结构

```
03_Work/AI-Team/
├── 00_品牌规范/                ← 按品牌划分，不可变，亚臣确认后才改
│   ├── 哈恩/
│   │   ├── 视觉规范.md
│   │   ├── 话语体系.md
│   │   └── 产品信息/
│   ├── 水星/
│   └── _品牌规范索引.md       ← 品牌列表 + 各品牌规范状态
├── 10_竞品档案/                ← Radar 可写
├── 20_战略决策记录/             ← Partner 可写
├── 30_交付物存档/               ← Workshop 可写（仅最终交付版 + 典型案例）
└── 40_审核规则库/               ← Keeper 可写
```

### 30_交付物存档/ 准入规则

交付物存档不是垃圾场。只有以下两类内容可以进入：

1. **最终交付版** — 经 Keeper 审核通过、亚臣确认的终稿
2. **典型案例** — 被明确判定为"值得复用的参考案例"的交付物，需标注复用价值

**不进入的内容：** 中间稿、探索性方案、被否决的版本、未经审核的草稿。这些留在 Workshop 本地或直接丢弃。

每个存档文件的 frontmatter 必须包含：

```yaml
---
type: 最终交付版 / 典型案例
project: [项目名]
date: YYYY-MM-DD
reuse_value: [一句话说明为什么值得存档]
---
```

### 品牌规范的双存在

- **Obsidian 文档 = 知识态**（人可读、可演化、版本化的源头）
- **Skill = 执行态**（给 AI 用的 prompt 规则，从文档编译而来）
- **流向：Obsidian 文档 → 成型后编译为 skill**

### 读写权限

| 目录 | 可读 | 可写 |
|------|------|------|
| 00_品牌规范/ | 所有工作 agent + Ally（只读） | 仅亚臣确认后 |
| 10_竞品档案/ | 所有工作 agent | Radar |
| 20_战略决策记录/ | 所有工作 agent + Ally（只读） | Partner |
| 30_交付物存档/ | 所有工作 agent | Workshop |
| 40_审核规则库/ | 所有工作 agent | Keeper |
| Butler | 不读共享层 | 不写共享层 |
| Ally | 可读品牌规范和战略决策（因为渗透工作） | 不写共享层 |

**Ally 的共享层原则：** Ally 的洞察如果涉及工作方向，由亚臣转达给 Partner，而不是 Ally 直接写入共享层。

---

## 九、记忆检索协议

写入每个 Agent 的 AGENTS.md，作为启动和响应前的标准流程：

```
记忆检索协议：
1. 启动时读 .abstract（L0 索引，<300 tokens）
2. 根据当前任务，从索引中识别相关文件
3. 读 MEMORY.md 中的相关 P 级条目（L1）
4. 如需更多上下文，读 lessons/ 或近期 reflections/（L2）
5. 共享层按需读取（只读与当前任务相关的品牌/领域）
6. 兜底：BGE-M3 语义检索（硅基流动免费接口）
7. 回答后把新关键信息写回 SESSION-STATE.md
8. session 结束时触发反思提取
```

### 各 Agent 的检索侧重

| Agent | L0 索引重点 | L1 重点 | 共享层重点 |
|-------|-----------|---------|-----------|
| Radar | 信息源清单、过滤经验 | 监控关键词、关注领域 | 竞品档案（检查重复） |
| Partner | 亚臣偏好、历史决策 | 决策风格、思维偏好 | 品牌规范、产品信息（对话素材） |
| Workshop | 生产标准、交付偏好 | brief 歧义点、品质标准 | 品牌规范、产品信息（执行依据） |
| Keeper | 审核标准、高频问题 | 被推翻的否决记录 | 法规规则库、品牌规范（审核依据） |
| Butler | 习惯、偏好、重要日期 | 支出分类规则 | 不读共享层 |
| Ally | 认知模式、成长轨迹 | 当前困惑主题、盲点类型 | 品牌规范、战略决策（只读，提供工作场景内省的上下文） |

---

## 十、反思机制

### 触发方式

| Agent | 触发方式 | 理由 |
|-------|---------|------|
| **Radar** | heartbeat 末尾挂载 | 已有定时触发 |
| **Butler** | heartbeat 末尾挂载 | 已有定时触发 |
| **Partner** | 每次有实质交互的 session 结束 | 按需触发，无事不记 |
| **Workshop** | 每次有实质交互的 session 结束 | 按需触发，无事不记 |
| **Keeper** | 每次有实质交互的 session 结束 | 按需触发，无事不记 |
| **Ally** | 每次有实质交互的 session 结束 | 按需触发，无事不记 |

**最小信息量门槛：** 模板中至少有一个区块有实质内容才写。无实质内容时不写。

### 反思模板（工作系统 + 生活系统）

```markdown
# YYYY-MM-DD 反思

## 信号强度：P0 / P1 / P2

## 意外（与预期不符的事）
- ...

## 修正（规则/标准需要调整）
- ...

## 沉淀（值得写入 MEMORY.md 的结论）
- ...  <!-- 写入前先读 MEMORY.md 检查重复/冲突 -->
```

### Ally 专用反思模板

```markdown
# YYYY-MM-DD 反思

## 信号强度：P0 / P1 / P2

## 认知变化（今天亚臣的认知有什么移动）
- ...

## 模式观察（反复出现的思维/情绪模式）
- ...

## 盲点线索（可能存在但尚未揭示的盲点）
- ...

## 沉淀（值得写入 MEMORY.md 的结论）
- ...  <!-- 写入前先读 MEMORY.md 检查重复/冲突 -->
```

### 流转机制

```
session 驱动 → reflections/YYYY-MM-DD.md（原始记录）
每周        → 回顾本周 reflections，提炼到 lessons/ + MEMORY.md
每月        → 扫描 P1/P2 过期条目 → archive/；刷新 .abstract
```

### CRUD 验证规则

写入 MEMORY.md 前必须：
1. 先读取现有内容
2. 检查是否重复（已有相同条目）
3. 检查是否冲突（与已有条目矛盾）
4. 冲突时：暴露冲突，不静默覆盖

---

## 十一、模型配置

| Agent | 主力模型 | 备选 | 理由 |
|-------|---------|------|------|
| **Radar** | GPT 5.4 | Gemini 3 Pro（后续考虑） | 长上下文扫描 + 摘要 |
| **Partner** | Claude Opus 4.6 | — | 思维深度、反直觉质疑 |
| **Workshop** | 按任务动态切换 | Opus(文案) / Codex(代码) / 专用生成模型(做图) | 多面手 |
| **Keeper** | GPT 5.4 | Claude Opus 4.6 / Gemini 3 Pro | 严谨推理 + 细节敏感 |
| **Butler** | GLM 5 | Gemini Flash / Kimi K2.5 | 日常琐事，省成本 |
| **Ally** | Claude Opus 4.6 | — | 情感细腻度、语言质感、深度共情 |
| **兜底** | GLM 5 | — | 低价值任务 |
| **向量检索** | BGE-M3（硅基流动免费） | — | 记忆语义检索 |

### 模型选择原则

```
感知（扫描大量信息）→ 长上下文模型
判断（深度思考）    → 最强推理模型
执行（多种产出）    → 按任务动态切换
校验（严谨审核）    → 强推理模型
运维（日常琐事）    → 轻量模型
内省（认知伙伴）    → 最强共情+语言模型
```

---

## 十二、自动化维护节奏

| 时机 | 动作 | 触发方式 |
|------|------|---------|
| 每次有价值的 session 结束 | 写 `reflections/YYYY-MM-DD.md` | session 驱动 |
| Radar/Butler heartbeat | 刷新 `SESSION-STATE.md` | cron |
| 每周 | `reflections/` → 提炼到 `lessons/` + `MEMORY.md` | 手动/脚本 |
| 每月 | 扫描 P1/P2 过期条目 → `archive/` | 脚本 |
| 每月 | 刷新 `.abstract` 索引 | 手动/脚本 |
| 每月 | 增量 BGE-M3 embedding 更新 | 脚本 |

---

## 十三、已讨论决策记录

以下问题已在讨论中确认方向：

| 原待讨论问题 | 决策 |
|-------------|------|
| 共享层位置 | Obsidian `03_Work/AI-Team/`，已同步 git |
| 品牌规范按品牌划分 | 是，`00_品牌规范/哈恩/`、`00_品牌规范/水星/` |
| 品牌规范双存在 | Obsidian 文档(源头) → skill(执行态) |
| 反思频率 | Radar/Butler 挂 heartbeat；其余 session 驱动，无事不记 |
| 反思质量 | 最小信息量门槛 + 偏差导向模板（只记意外/修正/沉淀） |
| 记忆膨胀 | P0/P1/P2 生命周期 + 每月归档过期条目 |
| 现有 MEMORY.md 迁移 | 直接重写（现有为空壳模板，无实际数据） |
| 记忆衰减 | verified 时间戳 + P1(90天)/P2(30天) 过期规则 |
| 记忆检索增强 | 先加上，使用 BGE-M3（硅基流动免费接口） |
| .abstract 索引 | 每个 Agent 加 L0 索引，启动时先读索引再按需读 |
| CRUD 验证 | 写入前先读、查重、查冲突 |
| Ally 归属 | 独立的第三系统（内在系统），4+1 升级为 4+1+1 |
| Ally 共享层权限 | 可读品牌规范和战略决策，不可写；洞察涉及工作方向时由亚臣转达 Partner |
| Ally 模型 | Claude Opus 4.6（情感细腻度 + 语言质感） |
| 三层私有记忆硬边界 | reflections(原始观察) / lessons(可复用经验) / MEMORY.md(稳定认知)，只晋升结论不复制内容，晋升后下层删除同义表述 |
| 交付物存档准入规则 | 仅最终交付版 + 典型案例可进入，中间稿/草稿不进入，需 frontmatter 标注复用价值 |

---

## 十四、落地计划（确认后执行）

1. 在 Obsidian `03_Work/` 下创建 `AI-Team/` 共享层目录结构
2. 在 AI-Team 仓库中创建 `Ally/` 目录及完整文件集（SOUL.md / IDENTITY.md / AGENTS.md / MEMORY.md / TOOLS.md）
3. 每个 Agent 目录（含 Ally）下创建 `.abstract` + `SESSION-STATE.md` + `reflections/` + `lessons/`
4. 重写 6 个 MEMORY.md，按 P0/P1/P2 分区（初始内容从现有 AGENTS.md 和 SOUL.md 中提取核心认知；Ally 从"知心伙伴 v6.0"和已有 OpenClaw 实例中提取）
5. 写 6 个 `.abstract` 初始索引
6. 更新 6 个 AGENTS.md：加入记忆检索协议、反思触发规则、模型配置
7. 更新 HEARTBEAT.md（Radar/Butler）：增加反思触发
8. 更新 AI-Team-Architecture.md 总纲：4+1+1 架构 + 共享记忆层 + 模型配置
9. 更新 USER.md / ROUTING.md：加入 Ally 的路由规则
10. 提交推送

---

_此文档为 v2 方案，整合社区实践经验 + Ally 归属决策，待亚臣最终确认后实施。_
